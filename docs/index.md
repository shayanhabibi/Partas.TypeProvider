# Partas.TypeProvider

Documentation home. `fsdocs` renders every `.md` and `.fsx` under `docs/`.

Build it with:

```shell
dotnet run --project Build.fsproj -- docs
```

Serve it with live reload:

```shell
dotnet run --project Build.fsproj -- docs --watch
```
